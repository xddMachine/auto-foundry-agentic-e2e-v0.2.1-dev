# v0.2.0 empirical baseline

- **Canonical source and proof.** The baseline is `auto-foundry-agentic-e2e` v0.2.0 at `/Users/fedorlebedev/Desktop/Dashboard/.auto-foundry-v020-transaction.d9AVEr/auto-foundry-agentic-e2e`. Its `SKILL.md` frontmatter, `README.md` heading/markers, and `TEST_PROMPTS.md` marker agree on `0.2.0`. The source contains 12 files; the imported tree is proven byte-exact by the post-import per-file SHA-256 comparison.
- **Run and provenance.** The empirical run is `/Users/fedorlebedev/Desktop/Dashboard/RUN-20260807-agentic-e2e-cleanroom-019fdd6a`; its immutable source ZIP SHA-256 is `82e9c913bf437ac9e361d6890467a9aed9b1c6db9d887cfcf0cd659035a71ec2` (`inputs/source_manifest.md`).
- **Repeated mechanical patterns.** Across the ten-question queue, analysis repeatedly used deterministic scripts, explicit grains/denominators, preserved draft/repaired outputs, and one bounded repair followed by a mechanical recheck for Q-002 through Q-010. Final products were assembled from reviewed outputs rather than silently re-running source analysis; Q-003 and Q-004 correctly retained blocked claims.
- **Ontology strength and schema drift.** The run promoted 53 source-local ontology items with evidence links and limits. Strong reusable grains, lineage, cutoff semantics, and proxy definitions were retained. Cross-system schema drift remains material: keys vary across `so_no`, `ecom_order_no`, `related_order_no`, delivery/tracking references, and manual-report identifiers; promise precedence, units, currencies, and shared cutoffs are not uniformly authoritative.
- **Reviewer/session limitation.** Fresh Sol review was unavailable for Q-007–Q-010 because of the host thread limit. Read-only Luna/max fallback rechecks are explicitly not independent Sol review (`products/final_run_report.md`, `products/product_manifest.json`).
- **Clean-room path limitation.** The first Q-005 worker read outside the allowlist, wrote zero current-run artifacts, and was discarded; the clean rerun used the exact allowlist. Absolute run paths and strict path allowlists therefore constrain portability and must remain visible in any rerun claim.
- **Dashboard limitations.** `products/management_dashboard.html` is a standalone, source-local snapshot; Playwright Chromium rendering passed, but no external publication or live refresh occurred. It is not an enterprise-authoritative KPI surface: proxy, blocked, currency-separated, coverage-window, and noncausal limits remain attached.
- **Optimizer misclassification.** `products/automation_candidates.md` mostly classifies client/business-process controls (promise precedence, unresolved handoff queue, receipt reconciliation, inventory snapshots, payment checks, AR snapshots, SLA monitors, and promotion scorecards) as automation candidates. That is the wrong target class for the experimental optimizer, which must separate Auto Foundry substrate/workflow optimization from client business automation; the path-allowlist/reviewer-availability item is the relevant environment/tooling exception.
- **Boundary.** This repository, the canonical source, and the successful run are separate: no run/source member was modified, and no raw data, implementation prompt, V3/runtime/canary/authorization repository, xddMachine, or autofoundary content is included or changed.
